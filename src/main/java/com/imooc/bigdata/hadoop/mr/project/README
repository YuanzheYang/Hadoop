统计页面的浏览量
    count
    一行记录 做成 一个固定的KEY，value赋值为1
统计各个省份的浏览量
        select province count(1) from  xxx group by province
        地市信息我们是可以 通过 ip解析 得到的 <== ip如何转换成城市信息
        ip解析：收费

统计页面的访问量：
    把符合规则的pageId获取到，然后进行 统计即可。